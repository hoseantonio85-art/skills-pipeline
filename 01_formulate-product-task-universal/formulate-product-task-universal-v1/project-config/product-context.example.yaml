version: 1

# Опциональный project-level выбор контекста.
# Если файл существует, skill использует context_id без дополнительного вопроса,
# пока пользователь явно не выбрал другой контекст.

context_id: norm
